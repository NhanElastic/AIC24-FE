import ImageResultView from "@/components/ImageResultView/index";
import { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof ImageResultView> = {
  component: ImageResultView,
};

export default meta;

type Story = StoryObj<typeof ImageResultView>;

export const Default: Story = {};
